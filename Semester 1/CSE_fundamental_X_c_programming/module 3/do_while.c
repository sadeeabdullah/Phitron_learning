#include<stdio.h>
int main(){
    int i = 1;
    do{
        printf("%d",i);
        i++;
    }while (i<=10);
    
    return 0;
}

// the code will run at least for once if the condition is not satisfied