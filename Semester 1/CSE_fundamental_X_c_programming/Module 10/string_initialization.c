#include<stdio.h>
int main(){
    //char s[6] = {'s','a','d','e','e'};
     // we can initialize the string like the array
    //N.B: we must have to keep a space for null otherwise it will show some garbage value
    char s[30] = "sadee abdullah"; //importantly we can have space in it.
    printf("%s",s);
    return 0;
}