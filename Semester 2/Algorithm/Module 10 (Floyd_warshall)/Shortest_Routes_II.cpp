#include <bits/stdc++.h>
using namespace std;

// we will use class for storing the a, b and c;
class sh
{
public:
    int a, b, c;

};
 vector<pair<int,int>> adj_list[505];

int main()
{
    int n, m, q;

    // creating pair of pari
    while (m--)
    {
        int a, b,c;
        cin >> a >> b >> c;
        sh.a = a;
        sh.b = b;
        sh.c = c;

        adj_list[a].push_back()
    }
    return 0;
}